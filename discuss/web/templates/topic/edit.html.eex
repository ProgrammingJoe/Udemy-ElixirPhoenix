<h1>Edit Topic</h1>

<%= form_for @changeset, topic_path(@conn, :update, @topic), fn f-> %>
  <div class="form-group">
    <%= text_input f, :title, placeholder: @topic.title, class: "form-control" %>
    <%= error_tag f, :title %>
  </div>

  <%= submit "Save", class: "btn btn-primary" %>
<% end %>
